// use entity::server_node;
// use sea_orm::{ActiveModelTrait, ActiveValue, DbConn, DbErr};

// // async fn register_server(db: &DbConn, name: String, url: String) -> Result<(), DbErr> {
// //     let server = server_node::ActiveModel {
        
// //     };
// //     server.insert(db).await?;
// //     Ok(())
// // }
