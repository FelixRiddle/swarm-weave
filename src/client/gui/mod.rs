use std::error::Error;
// use egui::{Context, CentralPanel};
// use eframe::{App, NativeOptions, init};

// struct MyApp {
//     // Your app state
// }

// impl MyApp {
//     fn new() -> Self {
//         Self {}
//     }
// }

// impl App for MyApp {
//     fn update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame) {
//         // Your app logic
//         CentralPanel::default().show(ctx, |ui| {
//             ui.label("Hello, world!");
//             // Your UI code
//         });
//     }
// }

/// Gui main window
/// 
/// 
pub async fn main() -> Result<(), Box<dyn Error>> {
    // let options = NativeOptions::default();
    // let mut ctx = Context::default();
    
    // init(ctx.clone(), |ctx| {
    //     let app = MyApp::new();
    //     eframe::run_native("My App", options, Box::new(app))
    // });
    
    Ok(())
}